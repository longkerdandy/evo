// Project Version
version = '1.0.0'

dependencies {
    // Evo
    compile project(':evo-api')

    // Arango
    compile('com.arangodb:arangodb-java-driver:2.4.1'){
        exclude module: 'commons-logging'
    }
    compile 'org.slf4j:jcl-over-slf4j:1.7.10'
}