// Project Version
version = '1.0.0'

dependencies {
    // Evo
    compile project(':evo-api')
    compile project(':evo-arangodb')

    // Netty
    compile 'io.netty:netty-all:4.0.25.Final'

    // UUID
    compile 'com.fasterxml.uuid:java-uuid-generator:3.1.4'
}