// Project Version
version = '1.0.0'

dependencies {
    // Evo-Redis
    compile project(':evo-api')
    compile project(':evo-redis')

    // Netty
    compile 'io.netty:netty-all:4.0.24.Final'
    compile 'io.netty:netty-codec-mqtt:4.1.0.Beta3'
}